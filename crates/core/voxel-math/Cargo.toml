[package]
name = "voxel-math"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.0", default-features = false }
fixed = "1.30"
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
