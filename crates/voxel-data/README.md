# Voxel Data

This crate owns the main voxel data. It has fast algorithms or modifying and querying the voxels in the Tree, Grid, and World. This is too allow complex abstractions like Subgrids so that most other crates do not need to think about them.
