[package]
name = "voxel-query"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true }
tile-data = { version = "0.1.0", path = "../tile-data" }
voxel-data = { version = "0.1.0", path = "../voxel-data" }

[lints]
workspace = true
